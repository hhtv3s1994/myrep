这里列举所有可能有的需求  包括不同领域  收集网络和现实见过的问题
各种业务需求问题
其实可以看成是个中文api大全   来自不同语言，不同领域的不同问题， 从底层到上层封装
只谈有哪些需求，不谈如何实现  也不讲概念细节
对于特别的需求，需要生造名词，抽象概念——不要用句子表示
以后可以考虑拆分成不同文件

分类：  语法基础  框架基础 前端 后端  游戏  算法   编译  爬虫子  动画特效   渗透   数学  
图形学



购物车   文章标题列表    垂直列表导航  水平导航  图片链接
 轮播图 
 分页链接  




类型的最大，最小值   类型获取   基本类型验证   对象类型验证  父类验证  
 隐式类型转换：赋值转型  传参转型 
转换为字符串   转换为高位数
强制类型转换： 基本类型强制转换  向上转型  向下转型   

遍历数组元素   遍历数组索引  反向遍历数组元素  从中间遍历   截断数组  
数组拼接为串 截取数组

字符串拼接  按符号分割为串数组  获取子串

函数：   无返回值函数   set型函数（修改型） get型函数   查找型函数   比较型  
参数：  传入对象  传入数组或者集合（有些语言集合数组字符串也是对象）   传入字符串  传入数字  
参数操作： 参数直接输出    参数赋值给其他变量   参数和其他变量运算再赋给其他变量  
修改数组元素  查找数组元素  增加数组元素  修改对象属性（不多见）      调用对象参数方法（委托）   调用集合数组字符串的自带对象方法
返回：   返回对错，是否   返回函数是否执行成功     返回计算值  返回查找值   返回过滤修改值  返回遍历结果

对象创建：构造创建  工厂化创建     析构函数  对象组合：set this传入组合  构造this传入组合
属性定义  属性初始化  定义时初始化 构造中初始化  init等方法中初始化   基本类型属性
组合对象属性   集合数组类属性   对象数组属性



遍历对象属性  方法     遍历原型
数字累加

条件：  

循环：计数循环 无限循环 遍历循环（利用某结构的长度）双重循环（双层遍历或者两次修改）  
循环其实就是范围缩小的重复——  范围缩小很重要，数组范围，对象范围，数据范围，树范围——双重循环范围缩小，遍历每个元素，深入遍历每个元素的内部结构
遍历枚举   遍历求最大值  遍历求和 遍历查找 遍历对比大小（分隔）  遍历分组（类型或者余数）   
逐步缩小范围再从头遍历   

 递归：单向递归   多向递归  直接递归  间接递归（间接调用自己）

日期时间： 格式化日期   获取时间戳   单独输出年月日  时分秒   时间戳转时分秒

函数定义   返回  参数类型验证  返回前验证

标准输入流，输出流 输入数字  输入字符   输出会被转换为字符串   文件读入到字符串 文件读入到数组   字符串写入到文件   数组写入到文件 

读文件  文件读取错误处理 

事件：获取鼠标位置  获取事件当前目标（冒泡委托） 获取按键键码  获取按键编码

常见题目：  
验证素数   水仙花数 求数字各位  转换为二进制   十六进制 求反码 补码（需要超出一位）
掩码 

转换为定长浮点数
生产者消费者   小型图书管理系统
俄罗斯方块  2048

前端：按钮  分界线  
导航栏  
CSS布局：   css并列的框   父子嵌套的框   运动的元素     交互的元素
背景颜色  背景
有边框 无边框
有阴影 无阴影  卡片式
交互：聚焦  鼠标悬浮  点击  
动画：永久播放  单次播放


动画特效：

游戏：  游戏循环：普通循环   异步循环：定时循环   帧循环 心跳循环     
每帧数据更新   每帧画面刷新（根据数据）  
