html解析 
css解析
把html和css都转为js对象？  dom对象挂载在哪

js解释  顺序遍历渲染所有  模块     对象属性   每次发生修改都进行重绘事件  部分重绘  全部重绘的逻辑   只重绘变动的部分

分层覆盖式重绘  如果覆盖的移开，还需要重绘不再被覆盖的部分   如何判断？根据坐标？

2、保存各层的数据，只修改被影响的层  某层发生变化，只重绘改变层和上一层？  但是可能有多层覆盖  3、简单的层间碰撞检测？

脏重绘