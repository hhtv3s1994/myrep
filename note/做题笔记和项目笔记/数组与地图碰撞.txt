从人物坐标获取地图的坐标

和地图上的静态物体发生碰撞

空间分区，把地图分割成数组区

分区计数器，每个区有几个对象？

对象由位置得到  分区的数组   如果在同一格就表示碰撞

对象有个属性 保存自己所在的数组

碰撞是人碰物体  还是物体碰人？  谁检测？

检测：分区中是否存在其他对象？有几个  每个空间树节点保存子节点拥有的对象数
对象本身保存自己前一次所在的数组   当自己的坐标超过数组时，更新数组状态