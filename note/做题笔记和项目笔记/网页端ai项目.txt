目的：制作一个ai画图程序 的网页版
工具：使用egret引擎
参考资料：  ai

ai是三阶    

碰撞检测:

清除与重绘
如何绘制图形的选择框？——算法？  还是直接循环尝试检测？   包围框和旋转角度是相同的


关于缩放和旋转会改变包围盒吗？  改变宽高会改变包围盒吗？  影响碰撞检测吗？ 对于对象整体大小有什么影响？   缩放时锚点坐标的影响？应该使用原始的函数吗？  还是使用函数重绘？
graphics画的跟对象的实际长度有关吗？
这些可以通过绘制边框，绘制

渲染过程：发生事件？  然后遍历显示列表，遍历属性进行渲染？   应该封装每个对象的渲染函数？  还是直接修改对象——egret似乎只要修改属性就能响应变化，不需要发送事件   但是还是要封装个改变的函数

灰度亮度：三种颜色相等，就是从黑到白
三种颜色混合组成混合色  ，然后加上  三个同样的三色值就是变亮或者变暗
红-粉红-赤红-褐红

选择：框选整个对象 或者多个对象  会用一个框框起来  九个点可以控制所有对象位置大小旋转
直接选择
直接选择工具：选择锚点   点击等于取消两个控制点   

选择的话线条会变色

旋转的话选择框会变化


采取图形对象的形式   利用交互控制对象的位置，大小，变换，层级
  渲染通过在图像

调色板模块:  由取色板获取颜色值   
区域填色是如何实现的？填充算法应该是遍历   如何给路径填充？

获取鼠标坐标()

默认状态创建一个新锚点并拖动，该锚点的两个控制点是同时转换并反向的。可以分开控制
新锚点的一个控制点是没用的是用来控制下一条线段的   所以每次只能拖动一条线的一个控制点
当控制点与锚点相等时，就不能拖动？


操作只有点击   换地方点击并拖动  两种  点击：创建锚点和控制点坐标相同   拖动，创建锚点控制点和坐标不同
控制点被拖动的是下一条线的控制点，坐标方向和长度跟随着鼠标，另一个点控制这条线
先实现一个钢笔工具试下？


锚点对象 ：   x y  cx1 cy1  cx2 cy2   注意锚点的其中一个控制点是影响前面一个是影响后面线段（与另一个锚点相互作用）    每个控制点也应该是对象，显示，隐藏  点击交互
按住中心旋转：两个控制点一起转   按住控制点旋转
锚点链条应该用链表相连

锚点  显示隐藏  可触摸
遍历 fill(color)  

普通对象继承精灵对象实现


包围盒：贝赛尔曲线的最高最低，最左最右点
0锚点：两个控制点在自身的锚点
中心点：  只作为参考线的参考标准    每个端点和中心点，还有包围框的横竖方向都有智能参考线，用来对齐

在画面直接点击时要求输入数值来生成

直线，两个0锚点   拉动生成
矩形  四个0锚点  一个中心点（非锚）
弧形：  开放  闭合  斜率（外 内弧）   这是怎么算的？        两个控制点垂直，控制点长度是？
  另一个控制点为0   
螺旋：720+180   900度的螺旋  每90度一个点 控制点和圆弧切   长度代表什么？
矩形网格   可以控制格子数
圆角矩形：六个点  控制点沿着切线
椭圆：四个锚点 一个中心点   控制点沿着切线

填充白色 和透明

橡皮，分割对象  线段，   ：控制点需要重新定位  ，这就需要知道线段生成贝赛尔如何拼接
橡皮会擦去一段   剪刀则是拦腰剪断

参数不写死


离屏渲染
