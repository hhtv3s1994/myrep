c  c++   堆    栈    全局静态 内存全局区
局部对象和数组和结构保存在哪  栈吗？
没错都保存在栈   局部指针也在栈中



参数传入对象数组结构的过程，返回的过程

指针指向地址  ，那么保存变量的地方是哪，栈吗？堆吗？ 可以是栈也可以是堆  直接定义就在栈
全局定义就在全局区    如果使用new的话就在堆 malloc也类似于堆，当成堆用没关系
  注意如果是堆中分配的即使退出函数也不会消除


main如果存在无限循环就不会终止，局部变量将一直在栈中  win程序大部分都不会自动终止
   


堆栈结构    分配原则  回收方式（因为对象分配在堆上，不能随着局部而消失?）

引用计数，不回收

java
js  引用类型都是保存在堆中，不能直接访问堆，是智能分配，自动清除  使用引用（地址值）访问

同样的引用对应同样的地址，同样的对象

执行上下文存在栈 中    “变量对象”存在堆，保存基本数据类型和引用类型的地址

定时找出不使用的对象，释放堆内存（局部变量）

闭包：返回函数或者对象，   如果返回的函数或者对象 使用了局部值，也不会释放局部值


python
