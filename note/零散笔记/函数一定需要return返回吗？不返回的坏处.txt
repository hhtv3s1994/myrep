汇编实际上是拆分成最小的命令单元


void需要返回吗？   如何平衡堆栈？     编译有默认的return吗？

void不需要写return,编译器有默认的return,如果不写会自动帮忙添上 

条件返回   当  if()  return这种形式可能出现多出口，多个return   可能某种情况会漏掉return


return执行后  后面的语句都不会执行


调用过程需要使用堆栈   返回调用入口地址，传入的参数，函数内部的变量，保存的bsp,和eax等，然后出栈时按照入栈顺序一个个从栈中删除
