节点 

解析html  匹配和提取各个节点

把每个节点抽象为对象，每个节点都有多个子节点  一层层形成树 

根是什么？  document


一个是dom节点    注释 元素，属性，文本
节点类型  节点属性（包括元素属性，和作为节点的特殊属性）


特殊属性  style,决定浏览器如何  渲染   所有节点都有默认属性和默认style
css  解析选择器进行匹配，修改各元素的样式属性，这需要遍历所有的html元素
if(element.class="" )element.style.size=   遍历法修改所有css

可以考虑解析形成css对象，不过对象之间似乎是无关的

css中用到不同的定位法，不同的坐标单位，渲染的时候应该采取统一的全局坐标，这里需要转换


事件与重绘   当样式改变时重绘：设置一个c消息循环，当收到样式改变时开始根据当前的信息重渲染


  按优先级规则  ，继承规则覆盖赋予各元素样式属性


渲染肯定需要遍历，如何遍历呢？   
需要确定整个框的大小 


js的解释器，运行时  设置数据   虚拟栈，堆 
vm吧   堆  栈简化用数组实现     寄存器就是c变量 
循环读写内存，运算。。。
   
最关键的js api部分    可以全部使用中断+c函数   实现  int  100     然后重绘


设置一个C绘制函数，根据内存中的对象值，来修改值    为dom对象开的内存可以是 
    动态创建所有图形
c  api   绘制方框   ，方框位置  ，方框大小，  方框边缘颜色
创建dom元素   获取dom元素


网络请求与响应的解析   下载的html,css,图片的存放

图片的解析 和显示


 然后通过这些数据来操控dom树的属性