数据耦合

依赖： new的对象   ，参数或者局部变量中用到另一个对象，静态方法，成员变量

访问另一个模块使用简单的数据参数

内容耦合

公共耦合 

标记耦合

外部耦合 ：一组模块访问同一全局简单变量（不是数组等结构），而且不是通过参数


两个类之间高度依赖   比如参数必须是某种类型（不能转型）
弱耦合  ：转型

解耦：
向上转型或者接口回调（一个意思）
增加中间层：比如适配器   使用适配对象的方法  修改时只用修改适配器中代码，当然适配器也是可以用接口和向上转型的

  
不管怎么样耦合是不会消除的，总有个地方你要改代码    感觉改参数最容易