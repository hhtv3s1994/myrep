要对代码的内容建库，采用文字目录或者图形目录



利用小函数拼装大函数 越小越好 

函数代码要尽可能小——然后多个小函数，组装抽象成api提供出来，如果组装的太复杂应该继续拆分

需要写中英文对照目录，要方便索引  排序，

零件，组件：  
底层也抽象
功能API

数值：

界面


2、常见代码功能块命名

增删查改

各种api  功能的使用场景命名

定义

数据结构    注意表格，结构体保存一串相关信息，游戏，web等领域很常用

   数组表  链表 ，结构，对象表   映射表，哈希表    字符串哈希  数字码哈希  
结构数组，保存多个表，多个对象。。。
对于重复同类数据表保存很有用




指针是右到左结合，作为类型返回值   先算右边。。。不用括号括起来
先优先级，再结合性，优先级相同看结合性，不同看优先级
单目
拆分子表达式

括号和点号优先级最高  然后是单目(指针地址，之后是算数  比较  赋值  位运算和逻辑运算  赋值  逗号


指针  函数指针   变量指针  数组指针  字符串指针     结构指针     字符串指针就是数组名
返回值指针  

指针   是指把变量p当指针使用，保存int型数据    所以 p可以替换成任何形式的变量，根据结合性可能需要打小括号     
 数组括号优先级高于指针   会先数组元素，后指向指针


使用小括号实现定义

指针有变量地址，数组名没有






定义变量 定义整数  定义字符串  定义对象 定义数组 定义二维数组  定义整数数组  定义字符串

数组   对象数组  指针数组  
字符数组和字符串和指针

类型判断是否  输出数据的类型

数组遍历   数组定义  访问索引元素  数组长度
数组排序  找最大最小值  查找元素索引   查找元素是否存在  判断索引处是否未定义  某个元素值  输出所有元素  按条件筛选输出部分元素(大于等于小于  修改元素值

入栈 出栈  入队 出队 遍历链表  循环取出队列头



对象遍历  属性是否存在  属性值   遍历间接获取对象属性名 ，方法名

双重循环  二次遍历 缩小范围遍历    二维遍历


字符串拼接  拼接插值数字  拼接换行符  拼接html   拼接sql  拼接函数

返回值

返回  返回数字 返回字符串  返回数组  返回对象  返回匿名对象引用  返回直接对象  返回布尔值  返回错误码 

返回指针的函数



传参调用  传入匿名对象 new  传入匿名函数
传入数组引用 传入对象属性  传入字符串  传入常数条件码     传入布尔条件码  
变量参数   字面量参数   直接传入取地址(用于向变量输出值 
传入数组名  函数名  指针名 结构名   字符数组名
 调用返回值赋值 
调用对象方法  内部创建对象方法  外部传入对象的方法
传入匿名对象字面量带属性  传入匿名回调函数 传入匿名数组字面量 



赋值   传入常数字面量     变量值传递
空数组 空字符串  空对象  
清空覆盖原值

初始化赋值  修改值  修改对象引用  修改数组引用  初始化对象属性， 修改对象属性     修改地址值 初始化数组元素 
变量交换
减一  减n  加一 加n   索引自增   
链表指针移动

表达式   数字相加  隐式转换    字符串后增
字符串内容变化  
字符串分割成数组  数组连接成字符串
  遍历字符串字符   查找子串




比较  数字比较  等于零  等于某数  不等于某数    数字范围  大于 小于   
字符串比较   
 
    

条件   比较语句   函数返回值is has exsist
   其他变量隐式转换为布尔变量  空字符串 数字0 空对象
条件语句   是否判断   存在判断  范围判断 
类型判断 对象类判断 属性存在判断 数组元素值判断 未定义元素判断 元素大小判断 空对象判断  未定义判断  空串判断 0判断 

长度对比   根据条件执行或不执行 根据是否判断执行哪个   根据类型码(数字或者串 执行不同语句（switch）     
顺序执行条件判断   if if
断路条件判断if else   switch break



循环  集合遍历循环 对象遍历循环  计数重复循环执行   循环中条件判断选择性执行  循环中跳过   循环中退出
